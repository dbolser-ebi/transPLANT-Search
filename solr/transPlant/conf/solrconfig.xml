<?xml version="1.0" encoding="UTF-8" ?>

<config>
  <luceneMatchVersion>LUCENE_42</luceneMatchVersion>
  
  <directoryFactory name="DirectoryFactory" class="${solr.directoryFactory:solr.StandardDirectoryFactory}" />
  
  <!-- These directories are relative to this file's location. -->
  <lib dir="../../../solr/contrib/extraction/lib" />
  <lib dir="../../../solr/contrib/dataimporthandler/lib/" regex=".*jar$" />
  
  <requestHandler name="standard" class="solr.StandardRequestHandler" default="true" />
  
  <requestHandler name="pinkPony" class="solr.SearchHandler" default="true" startup="lazy">
    
    <lst name="defaults">
      
      <!-- Add our shard servers... -->
      <str name="shards">localhost:${jetty.port:8983}/solr/transPlant-EBI,localhost:${jetty.port:8983}/solr/transPlant-IPK,localhost:${jetty.port:8983}/solr/transPlant-MIPS,localhost:${jetty.port:8983}/solr/transPlant-PAS,localhost:${jetty.port:8983}/solr/transPlant-UGRI</str>
      
      <!-- Print a summary of results per shard -->
      <str name="shards.info">true</str>
      
      <!-- Continue if one or more shards are down -->
      <str name="shards.tolerant">true</str>
      
      <!-- By default, don't spell check -->
      <str name="spellcheck">false</str>
      
      <!-- Defaults for the spell checker when used -->
      <str name="spellcheck.onlyMorePopular">true</str>
      <str name="spellcheck.extendedResults">false</str>
      
      <!--  The number of suggestions to return -->
      <str name="spellcheck.count">1</str>
      
    </lst>
    
    <!-- <arr name="last-components"> -->
    <!--   <str>spellcheck</str> -->
    <!-- </arr> -->
    
  </requestHandler>

  <searchComponent name="spellcheck" class="solr.SpellCheckComponent">
    
    <str name="queryAnalyzerFieldType">textSpell</str>
    
    <lst name="spellchecker">
      <str name="name">default</str>
      <str name="field">description</str>
      <str name="spellcheckIndexDir">spellchecker</str>
      <str name="buildOnOptimize">true</str>
    </lst>
  </searchComponent>
  
  <requestHandler name="/admin/" class="org.apache.solr.handler.admin.AdminHandlers" />
  <admin>
    <defaultQuery>*:*</defaultQuery>
  </admin>
  
  <requestHandler name="/admin/ping" class="solr.PingRequestHandler">
    <lst name="invariants">
      <str name="q">solrpingquery</str>
    </lst>
    <lst name="defaults">
      <str name="echoParams">all</str>
    </lst>
  </requestHandler>
  
  <requestHandler name="/update" class="solr.UpdateRequestHandler">
    <lst name="defaults">
      <str name="update.chain">dedupe</str>
    </lst>
  </requestHandler>
  
  <updateRequestProcessorChain name="dedupe">
    <processor class="solr.processor.SignatureUpdateProcessorFactory">
      <bool name="enabled">true</bool>
      <str name="signatureField">id</str>
      <bool name="overwriteDupes">false</bool>
      <str name="fields">database_name,db_id,db_version</str>
      <str name="signatureClass">solr.processor.Lookup3Signature</str>
    </processor>
    <processor class="solr.LogUpdateProcessorFactory" />
    <processor class="solr.RunUpdateProcessorFactory" />
  </updateRequestProcessorChain>
  
</config>
